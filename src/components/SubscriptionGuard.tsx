// Retained import for existing directory pages. Consultancy is billed per engagement;
// browsing the public catalogue does not require a paid membership.
export default function SubscriptionGuard({ children }: { children: React.ReactNode; previewSlot?: React.ReactNode }) {
  return <>{children}</>
}
